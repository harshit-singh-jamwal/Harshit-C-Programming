#include<stdio.h>
#include<math.h>
#include<stdlib.h>
#include<string.h>
int main(){
   int amount;
    scanf("%d",&amount);
    int count500,count100,count50,count20,count10,count5,count2,count1;
    if(amount>=500 ){
        count500=amount/500 ;
        amount=amount%500;
        printf("500: %d\n",count500 );
    }
    if(amount>=100 ){
        count100=amount/100 ;
        amount=amount%100 ;
        printf("100: %d\n",count100);
    }
    if(amount>=50 ){
        count50=amount/50 ;
        amount=amount%50 ;
        printf("50: %d\n",count50 );
    }
    if(amount>=20 ){
        count20=amount/20 ;
        amount=amount%20 ;
        printf("20: %d\n",count20 );
    }
    if(amount>=10 ){
        count10=amount/10 ;
        amount=amount%10 ;
        printf("10: %d\n",count10 );
    }
    if(amount>=5){
        count5=amount/5 ;
        amount=amount%5 ;
        printf("5: %d\n",count5 );
    }
    if(amount>=2 ){
        count2=amount/2 ;
        amount=amount%2 ;
        printf("2: %d\n",count2 );
    }
    if(amount>=1 ){
        count1=amount/1 ;
        amount=amount%1 ;
        printf("1: %d\n",count1 );
    }
    return 0;
}
