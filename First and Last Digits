#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int num,first,last;
    scanf("%d",&num);
     last=num%10;
    while(num>=10){
        num=num/10;
    }
    first=num;
    printf("%d %d",first,last);
    return 0;
}
